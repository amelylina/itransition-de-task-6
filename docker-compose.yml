services:
  postgres:
    image: postgres:16
    container_name: sqlfaker-pg
    restart: unless-stopped
    environment:
      POSTGRES_USER: faker
      POSTGRES_PASSWORD: faker
      POSTGRES_DB: sqlfaker
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
